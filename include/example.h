#pragma once

/**
 * This is a foo class to demonstrate features of the boiler plate.
 */
class Example {
	public:

  /**
   * Default constructor for foo (does nothing).
   */
   // int b = 1;
  Example();
  /**
   * Returns a bool.
   * @return Always True.
   */

  bool passVerifier(char* password);
};
